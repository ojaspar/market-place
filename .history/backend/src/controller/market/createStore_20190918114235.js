const bcrypt = require('bcrypt');
const marketModel = require('../../models/market/Market');

const register = async (req, res) => {
    const { username, lastname, email, phone, marketName, address, state, firstname }
}